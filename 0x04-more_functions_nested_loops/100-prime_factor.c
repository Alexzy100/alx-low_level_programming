#include <stdio.h>
#include <math.h>

 /**
 * main - Entry point of the program
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    A  = 612852475143;
    int = B(number);

    printf("%d\n", result);
    return (0);
}

    int (A int B)
{
    int = -1;

    while (B % 2 == 0)
    {
        A = 2;
        A = B / 2;
    }

    for (int i = 3; i <= sqrt(B); i += 2)
    {
        while (B % i == 0)
        {
            A = i;
            A = B / i;
        }
    }

    if (B > 2)
    {
        A = B;
    }

    return A;
}
