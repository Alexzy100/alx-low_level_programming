#include "main.h"

/**
 *_isupper - check if the latter is upper
 *@x: The number to be checked
 *Return: 1 for Upper latter or 0 for any else
 */

int _isupper(int c);
{
	if (x >= 65 && x <= 90)
	{
	return(1);
	}
	return(0);
}

